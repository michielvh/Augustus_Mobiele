import { List, Map } from 'immutable';

 const init = {
   personen:[{naam:'jan'},{naam:'loe'},{naam:'fred'},{naam:'barry'},{naam:'kees'},{naam:'bart'},{naam:'ronny'},{naam:'xoxo'},{naam:'gabber'}],
  
 }
 const reducer = (state=init, action) => {
    return state;
  }
  
  export default reducer